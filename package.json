{
  "name": "codex",
  "version": "1.0.0",
  "description": "This version uses a real backend with Supabase (Auth + Postgres) instead of browser-only local storage.",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0"
  }
}
